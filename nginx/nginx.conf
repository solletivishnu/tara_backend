server {
    listen 80 default_server;
    server_name 13.233.131.204;

    # Timeout settings
    proxy_connect_timeout 1100s;
    proxy_send_timeout 1100s;
    proxy_read_timeout 1100s;
    send_timeout 1100s;

    # Favicon
    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    # Static files
    location /staticfiles/ {
        alias /home/ubuntu/TaraFirst/staticfiles/;  # Use 'alias' instead of 'root' for static files
    }

    # Proxy to Gunicorn
    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}
